bin.name = "conda-dist"
args = ["--locked", "lock", "portable-bash.toml"]
status.code = 1
stderr = "Error: lockfile required by --locked but not found at [CWD]/portable-bash.lock; generate it with --unlock\n"

[fs]
cwd = "../fixtures/lock-missing"
